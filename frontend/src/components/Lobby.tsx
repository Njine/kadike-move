import React from "react";

const Lobby: React.FC = () => {
  return (
    <div className="p-4">
      <h2 className="text-2xl font-bold mb-4">Lobby</h2>
      <div className="mb-2">Players can join here and see the match pool.</div>
      {/* TODO: Add player join form and match pool list */}
    </div>
  );
};

export default Lobby;
